# Test server
```sh
cd server
cargo run
```
# Test client
First install `tauri-cli`:
```sh
cargo install tauri-cli --version "^2.0.0" --locked
```
Then run:
```sh
cd client
cargo tauri dev
```
